kind = "web"
previewPath = "/"
title = "TARA — AI Travel Assistant"
version = "1.0.0"
id = "artifacts/tara"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 19618

[services.development]
run = "pnpm --filter @workspace/tara run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/tara", "run", "build" ]
publicDir = "artifacts/tara/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "19618"
BASE_PATH = "/"
